# FastAPI Chatbot Requirements

# Core FastAPI
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic>=2.7.4

# Core LangGraph (matching your versions)
langgraph==0.2.28
langchain-core==0.3.1
langchain-ollama==0.2.0

# Vector DB & Embeddings (matching your versions)
faiss-cpu==1.13.0
numpy>=1.26.4

# Ollama (matching your version)
ollama==0.3.3

# Web Scraping (for dynamic content - matching your versions)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0

# Async HTTP for API calls (matching your version)
aiohttp>=3.9.0

# Redis for distributed memory and caching
redis>=5.0.0
hiredis>=2.3.0  # Optional: C parser for better performance

# Environment variables (matching your requirement)
python-dotenv>=1.0.0

# UI - Optional (if you want to keep Gradio alongside FastAPI)
# gradio>=6.0.1
# pillow>=12.0.0

# For production
python-multipart==0.0.6
python-jose[cryptography]==3.3.0  # Optional: if you need JWT authentication